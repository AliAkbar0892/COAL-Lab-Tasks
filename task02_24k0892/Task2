INCLUDE Irvine32.inc

.code
main PROC

mov eax, 47
mov ebx, 39
mov ecx, 60

add eax, ebx
add eax, ecx

mov ebx, 85
mov ecx, 64

add eax, ebx
add eax, ecx

mov ebx, 44
mov ecx, 0Ah

add eax, ebx
sub eax, ecx


call DumpRegs

exit
main ENDP
END main
